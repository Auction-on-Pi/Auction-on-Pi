model User {
  id        Int      @id @default(autoincrement())
  piUserId  String   @unique
  username  String
  roles     String[] @default(["user"])  // Added roles field
  auctions  Auction[]
  bids      Bid[]
  createdAt DateTime @default(now())     // Added timestamps
  updatedAt DateTime @updatedAt
}

// Rest of your schema remains the same
